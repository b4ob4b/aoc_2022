import utils.Day
import utils.IO

fun main() {
    Day10(IO.TYPE.SAMPLE).test()
    Day10().solve()
}

class Day10(inputType: IO.TYPE = IO.TYPE.INPUT) : Day("", inputType = inputType) {
    override fun part1(): Any? {
        return "not yet implement"
    }

    override fun part2(): Any? {
        return "not yet implement"
    }
}           