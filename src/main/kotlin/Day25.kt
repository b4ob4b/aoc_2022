import utils.Day
import utils.IO

fun main() {
    Day25(IO.TYPE.SAMPLE).test()
    Day25().solve()
}

class Day25(inputType: IO.TYPE = IO.TYPE.INPUT) : Day("", inputType = inputType) {
    override fun part1(): Any? {
        return "not yet implement"
    }

    override fun part2(): Any? {
        return "not yet implement"
    }
}           